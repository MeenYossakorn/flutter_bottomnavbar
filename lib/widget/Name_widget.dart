import 'package:flutter/material.dart';

class Name_widget extends StatelessWidget {
  const Name_widget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Yossakorn Lengrabum",
      style: TextStyle(
        fontFamily: 'poppins',
        fontSize: 20,
        color: Colors.black,
      ),
    );
  }
}
